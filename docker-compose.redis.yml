services:
  redis:
    image: redis:7-alpine
    container_name: crm-support-redis
    command: redis-server --appendonly yes
    ports:
      - '${REDIS_PORT:-6379}:6379'
    volumes:
      - redis_data:/data
    restart: unless-stopped

volumes:
  redis_data:
