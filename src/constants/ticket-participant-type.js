export const TICKET_PARTICIPANT_TYPE = Object.freeze({
  WATCHER: 'watcher',
  COLLABORATOR: 'collaborator',
});

export const TICKET_PARTICIPANT_TYPE_VALUES = Object.freeze(
  Object.values(TICKET_PARTICIPANT_TYPE)
);
