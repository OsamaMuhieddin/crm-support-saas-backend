export const BILLING_ADDON_TYPE = Object.freeze({
  SEAT: 'seat',
  USAGE: 'usage',
  FEATURE: 'feature'
});

export const BILLING_ADDON_TYPE_VALUES = Object.freeze(
  Object.values(BILLING_ADDON_TYPE)
);

