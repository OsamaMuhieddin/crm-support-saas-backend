export const TICKET_PRIORITY = Object.freeze({
  LOW: 'low',
  NORMAL: 'normal',
  HIGH: 'high',
  URGENT: 'urgent'
});

export const TICKET_PRIORITY_VALUES = Object.freeze(Object.values(TICKET_PRIORITY));

