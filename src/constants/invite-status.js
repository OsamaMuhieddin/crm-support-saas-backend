export const INVITE_STATUS = Object.freeze({
  PENDING: 'pending',
  ACCEPTED: 'accepted',
  REVOKED: 'revoked',
  EXPIRED: 'expired'
});

export const INVITE_STATUS_VALUES = Object.freeze(Object.values(INVITE_STATUS));

